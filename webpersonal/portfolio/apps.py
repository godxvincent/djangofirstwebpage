from django.apps import AppConfig


class PortfolioConfig(AppConfig):
    name = 'portfolio'
    # En esta clase se pueden adicionar atributos de configuración de la app.
    verbose_name = 'Portafolio'
